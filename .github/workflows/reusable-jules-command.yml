name: Reusable Jules Command

on:
  workflow_call:
    inputs:
      comment_body:
        required: true
        type: string
      issue_number:
        required: true
        type: string
      comment_id:
        required: true
        type: string
      repository_owner:
        required: true
        type: string
      repository_name:
        required: true
        type: string
    secrets:
      JULES_API_KEY:
        required: true
      JULES_API_URL:
        required: true
      GH_TOKEN:
        required: true

jobs:
  jules-command:
    runs-on: self-hosted
    env:
      PR_NUMBER_RAW: ${{ inputs.issue_number || 0 }}
    steps:
      - name: Validate Context
        id: validate
        run: |
          if [ "$PR_NUMBER_RAW" == "0" ] || [ -z "$PR_NUMBER_RAW" ]; then
            echo "::warning::Missing or invalid PR_NUMBER ($PR_NUMBER_RAW). Skipping job."
            echo "should_run=false" >> $GITHUB_OUTPUT
          else
            echo "should_run=true" >> $GITHUB_OUTPUT
          fi

      - name: Checkout repository
        if: steps.validate.outputs.should_run == 'true'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        if: steps.validate.outputs.should_run == 'true'
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 'Parse Command'
        if: steps.validate.outputs.should_run == 'true'
        id: parse_command
        env:
          COMMENT_BODY: ${{ inputs.comment_body }}
        run: |
          if [[ "$COMMENT_BODY" == "@jules-delete"* ]]; then
            echo "COMMAND=delete" >> $GITHUB_ENV
          elif [[ "$COMMENT_BODY" == "@jules-new"* ]]; then
            echo "COMMAND=new" >> $GITHUB_ENV
          fi

      - name: 'Get PR Context'
        id: pr_context
        if: steps.validate.outputs.should_run == 'true' && env.COMMAND == 'new'
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          PR_NUMBER: ${{ inputs.issue_number }}
        run: |
          if [ -z "$PR_NUMBER" ] || [ "$PR_NUMBER" == "0" ]; then
            echo "::warning::PR_NUMBER is missing or 0. Skipping."
            exit 0
          fi

          # Fetch basic PR data
          PR_DATA=$(gh pr view "$PR_NUMBER" --json headRefName,baseRefName,body,title,comments,reviews)
          BRANCH=$(echo "$PR_DATA" | jq -r .headRefName)
          BASE_REF=$(echo "$PR_DATA" | jq -r .baseRefName)
          BODY=$(echo "$PR_DATA" | jq -r .body)
          TITLE=$(echo "$PR_DATA" | jq -r .title)

          # Fetch and format comments
          COMMENTS=$(echo "$PR_DATA" | jq -r '.comments | .[] | "- \(.body)"' | sed 's/^/  /')
          REVIEWS=$(echo "$PR_DATA" | jq -r '.reviews | .[] | select(.body != "") | "- \(.body)"' | sed 's/^/  /')

          # Fetch git logs
          # We assume the PR is up-to-date with the base branch
          # This gets the logs from the point the branch was created
          MERGE_BASE=$(git merge-base "origin/$BASE_REF" "HEAD")
          LOGS=$(git log -n 50 --pretty=format:"- %s" "$MERGE_BASE..HEAD")

          # Construct the full prompt
          FULL_PROMPT=$(cat <<EOM
          **PR Title:** $TITLE

          **PR Body:**
          $BODY

          **Review Comments:**
          $REVIEWS

          **Other Comments:**
          $COMMENTS

          **Git Logs:**
          $LOGS
          EOM
          )

          echo "BRANCH=$BRANCH" >> $GITHUB_ENV
          echo "PROMPT<<EOF" >> $GITHUB_ENV
          echo "$FULL_PROMPT" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
          echo "TITLE=$TITLE" >> $GITHUB_ENV

      - name: 'Find Jules Session ID'
        id: find_session_id
        if: steps.validate.outputs.should_run == 'true' && env.COMMAND == 'delete'
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          PR_NUMBER: ${{ inputs.issue_number }}
        run: |
          if [ -z "$PR_NUMBER" ] || [ "$PR_NUMBER" == "0" ]; then
            echo "::warning::PR_NUMBER is missing or 0. Skipping."
            exit 0
          fi
          SESSION_ID=$(gh pr view "$PR_NUMBER" --json comments --jq '.comments | .[] | .body' | grep -oP '(?<=<!-- JULES_SESSION_ID=)\S+(?= *-->)' | tail -n 1)
          if [ -z "$SESSION_ID" ]; then
            echo "::error::Could not find Jules session ID in PR comments. Was a session previously created using '@jules-new' and completed successfully?"
            exit 1
          fi
          echo "JULES_SESSION_ID=$SESSION_ID" >> $GITHUB_ENV

      - name: 'Validate Secrets and Install Dependencies'
        if: steps.validate.outputs.should_run == 'true'
        env:
          JULES_API_URL: ${{ secrets.JULES_API_URL }}
          JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
        run: |
          set +e
          missing_vars=""

          if [ -z "$JULES_API_URL" ]; then
            missing_vars="JULES_API_URL"
          fi

          if [ -z "$JULES_API_KEY" ]; then
            if [ -n "$missing_vars" ]; then
              missing_vars="$missing_vars, JULES_API_KEY"
            else
              missing_vars="JULES_API_KEY"
            fi
          fi

          if [ -n "$missing_vars" ]; then
            echo "::error::The following required secrets are not set: $missing_vars. Please configure them in your repository secrets."
            exit 1
          fi

          echo "✅ All required Jules secrets are configured"
          pip install -r .github/scripts/requirements.txt

      - name: 'Run Jules Operation: New'
        if: steps.validate.outputs.should_run == 'true' && env.COMMAND == 'new'
        id: jules_op_new
        env:
          JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
          BRANCH: ${{ env.BRANCH }}
          TITLE: ${{ env.TITLE }}
          PROMPT: ${{ env.PROMPT }}
          REPO_OWNER: ${{ inputs.repository_owner }}
          REPO_NAME: ${{ inputs.repository_name }}
        run: |
          set +e
          OUTPUT=$(python3 .github/scripts/jules_ops.py \
            --command "new" \
            --prompt "$PROMPT" \
            --branch "$BRANCH" \
            --title "$TITLE" \
            --owner "$REPO_OWNER" \
            --repo-name "$REPO_NAME" \
            --jules-api-url "${{ secrets.JULES_API_URL }}" 2>&1)
          EXIT_CODE=$?

          if [ $EXIT_CODE -ne 0 ]; then
            echo "error_message=$OUTPUT" >> $GITHUB_OUTPUT
            exit $EXIT_CODE
          fi

          echo "session_id=$OUTPUT" >> $GITHUB_OUTPUT

      - name: 'Run Jules Operation: Delete'
        if: steps.validate.outputs.should_run == 'true' && env.COMMAND == 'delete'
        id: jules_op_delete
        env:
          JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
          JULES_SESSION_ID: ${{ env.JULES_SESSION_ID }}
        run: |
          set +e
          OUTPUT=$(python3 .github/scripts/jules_ops.py \
            --command "delete" \
            --session-id "$JULES_SESSION_ID" \
            --jules-api-url "${{ secrets.JULES_API_URL }}" 2>&1)
          EXIT_CODE=$?

          if [ $EXIT_CODE -ne 0 ]; then
            echo "error_message=$OUTPUT" >> $GITHUB_OUTPUT
            exit $EXIT_CODE
          fi

      - name: 'Post Session ID Comment'
        if: steps.validate.outputs.should_run == 'true' && success() && env.COMMAND == 'new'
        uses: actions/github-script@v7
        env:
          PR_NUMBER_INPUT: ${{ inputs.issue_number }}
          SESSION_ID: ${{ steps.jules_op_new.outputs.session_id }}
        with:
          github-token: ${{ secrets.GH_TOKEN }}
          script: |
            const prNumber = Number(process.env.PR_NUMBER_INPUT) || (context.payload.pull_request && context.payload.pull_request.number) || context.issue.number || 0;

            const sessionId = process.env.SESSION_ID;
            if (sessionId && prNumber && prNumber !== 0) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: `<!-- JULES_SESSION_ID=${sessionId} -->`
              });
            }

      - name: 'Add Reaction to Comment'
        if: steps.validate.outputs.should_run == 'true' && success() && ((github.event.comment && github.event.comment.id) || (inputs.comment_id && inputs.comment_id != '0' && inputs.comment_id != 0))
        uses: peter-evans/create-reaction@v4
        with:
          token: ${{ secrets.GH_TOKEN }}
          comment-id: ${{ (github.event.comment && github.event.comment.id) || inputs.comment_id || 0 }}
          reaction: '+1'
      - name: 'Handle Failure'
        if: steps.validate.outputs.should_run == 'true' && failure()
        uses: actions/github-script@v7
        env:
          PR_NUMBER_INPUT: ${{ inputs.issue_number }}
          COMMENT_ID_INPUT: ${{ inputs.comment_id }}
          ERROR_NEW: ${{ steps.jules_op_new.outputs.error_message }}
          ERROR_DELETE: ${{ steps.jules_op_delete.outputs.error_message }}
        with:
          github-token: ${{ secrets.GH_TOKEN }}
          script: |
            const prNumber = Number(process.env.PR_NUMBER_INPUT) || (context.payload.pull_request && context.payload.pull_request.number) || context.issue.number || 0;
            const commentId = Number(process.env.COMMENT_ID_INPUT) || context.payload.comment?.id || 0;

            const errorMessageNew = process.env.ERROR_NEW || '';
            const errorMessageDelete = process.env.ERROR_DELETE || '';
            const errorMessage = errorMessageNew || errorMessageDelete || 'Unknown error occurred';

            const body = `Jules operation failed with the following error:\n\n\`\`\`\n${errorMessage}\n\`\`\``;

            if (commentId && commentId !== 0) {
              try {
                await github.rest.reactions.createForIssueComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  comment_id: commentId,
                  content: '-1'
                });
              } catch (e) {
                console.log('Could not add reaction:', e);
              }
            }

            if (prNumber && prNumber !== 0) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: body
              });
            }
