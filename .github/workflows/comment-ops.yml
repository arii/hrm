name: Bot Command Orchestrator

on:
  issue_comment:
    types: [created]

concurrency:
  group: ${{ github.workflow }}-${{ (github.event.issue && github.event.issue.number) || github.run_id }}
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write
  issues: write
  checks: write
  actions: write

jobs:
  router:
    name: 🔍 Route Comment
    if: github.event.issue && github.event.issue.pull_request
    runs-on: ubuntu-latest
    outputs:
<<<<<<< HEAD
      is_review: ${{ steps.route.outputs.is_review }}
      is_squash: ${{ steps.route.outputs.is_squash }}
      is_resolve: ${{ steps.route.outputs.is_resolve }}
      pr_number: ${{ steps.route.outputs.pr_number }}
      comment_id: ${{ steps.route.outputs.comment_id }}
=======
      is_review: ${{ steps.route.outputs.is_review || 'false' }}
      is_squash: ${{ steps.route.outputs.is_squash || 'false' }}
      is_resolve: ${{ steps.route.outputs.is_resolve || 'false' }}
>>>>>>> origin/leader
    steps:
      - id: route
        env:
          BODY: ${{ (github.event.comment && github.event.comment.body) || '' }}
          PR_NUMBER: ${{ (github.event.issue && github.event.issue.number) || 0 }}
          COMMENT_ID: ${{ (github.event.comment && github.event.comment.id) || 0 }}
        run: |
<<<<<<< HEAD
          if echo "$BODY" | grep -q "^@gemini-bot"; then echo "is_review=true" >> $GITHUB_OUTPUT; fi
          if echo "$BODY" | grep -q "@pr-squash"; then echo "is_squash=true" >> $GITHUB_OUTPUT; fi
          if echo "$BODY" | grep -q "@conflict-resolve"; then echo "is_resolve=true" >> $GITHUB_OUTPUT; fi
          echo "pr_number=$PR_NUMBER" >> $GITHUB_OUTPUT
          echo "comment_id=$COMMENT_ID" >> $GITHUB_OUTPUT
=======
          # Use case-insensitive grep and ensure outputs are always set if matched.
          # We check for commands starting at the beginning of a line to avoid accidental triggers
          # from quotes or descriptive text mentioning the bots.
          if echo "$BODY" | grep -qiE "^@gemini-bot"; then echo "is_review=true" >> $GITHUB_OUTPUT; fi
          if echo "$BODY" | grep -qiE "(^|\r?\n)@pr-squash"; then echo "is_squash=true" >> $GITHUB_OUTPUT; fi
          if echo "$BODY" | grep -qiE "(^|\r?\n)@conflict-resolve"; then echo "is_resolve=true" >> $GITHUB_OUTPUT; fi
>>>>>>> origin/leader

  execute-review:
    needs: [router]
    if: needs.router.outputs.is_review == 'true' && needs.router.outputs.pr_number != '0'
    uses: ./.github/workflows/reusable-gemini-review.yml
    with:
      pr_quality_result: 'not_applicable'
      trigger_event: 'comment'
      comment_body: ${{ (github.event.comment && github.event.comment.body) || '' }}
      pr_number: ${{ needs.router.outputs.pr_number }}
    secrets: inherit

  execute-squash:
    needs: [router]
    if: needs.router.outputs.is_squash == 'true' && needs.router.outputs.pr_number != '0'
    uses: ./.github/workflows/pr-squash.yml
    with:
      pr_number: ${{ needs.router.outputs.pr_number }}
      comment_body: ${{ (github.event.comment && github.event.comment.body) || '' }}
      comment_id: ${{ needs.router.outputs.comment_id }}
    secrets: inherit

  prepare-resolve:
    needs: [router]
    if: needs.router.outputs.is_resolve == 'true' && needs.router.outputs.pr_number != '0'
    runs-on: ubuntu-latest
    outputs:
      target_branch: ${{ steps.get_branches.outputs.target_branch }}
      source_branch: ${{ steps.get_branches.outputs.source_branch }}
      should_run: ${{ steps.validate.outputs.should_run }}
    steps:
      - name: Validate Context
        id: validate
        run: |
          PR_NUMBER=${{ needs.router.outputs.pr_number }}
          if [ "$PR_NUMBER" == "0" ] || [ -z "$PR_NUMBER" ]; then
            echo "::warning::Missing or invalid PR_NUMBER ($PR_NUMBER). Skipping job."
            echo "should_run=false" >> $GITHUB_OUTPUT
          else
            echo "should_run=true" >> $GITHUB_OUTPUT
          fi

      - name: Add reaction to comment
        if: steps.validate.outputs.should_run == 'true' && needs.router.outputs.comment_id != '0'
        uses: peter-evans/create-reaction@v4
        with:
<<<<<<< HEAD
          token: ${{ secrets.GITHUB_TOKEN }}
          comment-id: ${{ needs.router.outputs.comment_id }}
          reaction: eyes

=======
          script: |
            github.rest.reactions.createForIssueComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: context.payload.comment.id,
              content: 'eyes'
            });
>>>>>>> origin/leader
      - name: Get PR Details
        if: steps.validate.outputs.should_run == 'true'
        id: get_branches
        uses: actions/github-script@v7
        env:
          PR_NUMBER: ${{ needs.router.outputs.pr_number }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = parseInt(process.env.PR_NUMBER) || 0;
            if (!prNumber) {
              console.log('Could not determine PR number. Skipping.');
              return;
            }
            const pr = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
            });
            core.setOutput('target_branch', pr.data.base.ref);
            core.setOutput('source_branch', pr.data.head.ref);

  execute-resolve:
    needs: [router, prepare-resolve]
    if: needs.router.outputs.is_resolve == 'true' && needs.prepare-resolve.outputs.should_run == 'true'
    uses: ./.github/workflows/conflict-resolver.yml
    with:
      target_branch: ${{ needs.prepare-resolve.outputs.target_branch }}
      source_branch: ${{ needs.prepare-resolve.outputs.source_branch }}
      pr_number: ${{ needs.router.outputs.pr_number }}
    secrets: inherit
