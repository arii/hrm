name: 'E2E CI Tests'

on:
  push:
    paths:
      - '.github/**'

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  test_pr_squash:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}
          fetch-depth: 0

      - name: Create Test Branch
        run: |
          set -e
          BRANCH_NAME="e2e-test-$(date +%s)"
          echo "BRANCH_NAME=$BRANCH_NAME" >> $GITHUB_ENV
          git checkout -b $BRANCH_NAME
          echo "test content" > test-file.txt
          git add test-file.txt
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit -m "E2E Test Commit 1"
          echo "another line" >> test-file.txt
          git add test-file.txt
          git commit -m "E2E Test Commit 2"
          git push origin $BRANCH_NAME

      - name: Create PR
        id: create_pr
        uses: ./.github/actions/gh-pr-create
        with:
          title: 'E2E Test PR'
          body: 'Test PR for E2E testing'
          base: leader
          head: ${{ env.BRANCH_NAME }}
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Get Initial SHA
        id: get_sha
        env:
          BRANCH_NAME: ${{ env.BRANCH_NAME }}
        run: |
          set -e
          # git ls-remote origin queries the server directly and is the most reliable method in this context
          SHA=$(git ls-remote origin "refs/heads/$BRANCH_NAME" | awk '{print $1}')
          if [ -z "$SHA" ]; then
            echo "❌ Error: Could not get initial SHA for $BRANCH_NAME"
            exit 1
          fi
          echo "initial_sha=$SHA" >> $GITHUB_OUTPUT

      - name: Trigger PR Squash
        uses: ./.github/actions/gh-pr-comment
        with:
          pr_number: ${{ steps.create_pr.outputs.pr_number }}
          body: '@pr-squash'
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Wait for PR Update
        uses: ./.github/actions/gh-wait-for-branch-update
        with:
          branch: ${{ env.BRANCH_NAME }}
          initial_sha: ${{ steps.get_sha.outputs.initial_sha }}
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Wait for Checks
        uses: ./.github/actions/gh-pr-wait-checks
        with:
          pr_number: ${{ steps.create_pr.outputs.pr_number }}
          timeout: 1200
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Verify Squash
        env:
          GH_TOKEN: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}
          BRANCH_NAME: ${{ env.BRANCH_NAME }}
        run: |
          set -e
          git fetch origin $BRANCH_NAME
          # Use FETCH_HEAD to ensure we are checking out exactly what was just fetched,
          # bypassing any potentially stale remote-tracking references.
          git checkout -B $BRANCH_NAME FETCH_HEAD
          COMMIT_COUNT=$(git rev-list --count HEAD ^origin/leader)
          if [ "$COMMIT_COUNT" -eq 1 ]; then
            echo "✅ Squash successful: Branch has 1 commit."
          else
            echo "❌ Squash failed: Branch has $COMMIT_COUNT commits instead of 1."
            exit 1
          fi

      - name: Close PR
        if: always()
        uses: ./.github/actions/gh-pr-close
        with:
          pr_number: ${{ steps.create_pr.outputs.pr_number }}
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Cleanup Branch
        if: always()
        env:
          GH_TOKEN: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}
          BRANCH_NAME: ${{ env.BRANCH_NAME }}
        run: |
          set -e
          echo "Cleaning up branch..."
          git push origin --delete $BRANCH_NAME

  test_conflict_resolver:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}
          fetch-depth: 0

      - name: Create Conflict
        run: |
          set -e
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Use a unique identifier for branch names to avoid concurrency issues
          UNIQUE_ID=${{ github.run_id }}
          BRANCH_A="branch-a-$UNIQUE_ID"
          BRANCH_B="branch-b-$UNIQUE_ID"
          BASE_BRANCH="base-branch-$UNIQUE_ID"

          echo "BRANCH_A=$BRANCH_A" >> $GITHUB_ENV
          echo "BRANCH_B=$BRANCH_B" >> $GITHUB_ENV
          echo "BASE_BRANCH=$BASE_BRANCH" >> $GITHUB_ENV

          # Create a base commit with a file
          # We use a filename matching *.generated.ts to ensure it's handled by the conflict resolver bot
          FILENAME="conflict-file.generated.ts"
          git checkout -b $BASE_BRANCH
          echo "initial content" > $FILENAME
          git add $FILENAME
          git commit -m "Create $FILENAME"
          git push origin $BASE_BRANCH

          # Create branch A with a modification
          git checkout -b $BRANCH_A
          echo "branch-a content" > $FILENAME
          git add $FILENAME
          git commit -m "Modify $FILENAME in branch-a"
          git push origin $BRANCH_A

          # Create branch B from the base, with a conflicting modification
          git checkout $BASE_BRANCH
          git checkout -b $BRANCH_B
          echo "branch-b content" > $FILENAME
          git add $FILENAME
          git commit -m "Modify $FILENAME in branch-b"
          git push origin $BRANCH_B

      - name: Create PR
        id: create_conflict
        uses: ./.github/actions/gh-pr-create
        with:
          title: 'E2E Test PR with Conflict'
          body: 'Test PR for conflict resolver'
          base: ${{ env.BRANCH_A }}
          head: ${{ env.BRANCH_B }}
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Get Initial SHA
        id: get_sha
        env:
          BRANCH_B: ${{ env.BRANCH_B }}
        run: |
          set -e
          SHA=$(git ls-remote origin "refs/heads/$BRANCH_B" | awk '{print $1}')
          if [ -z "$SHA" ]; then
            echo "❌ Error: Could not get initial SHA for $BRANCH_B"
            exit 1
          fi
          echo "initial_sha=$SHA" >> $GITHUB_OUTPUT

      - name: Trigger Conflict Resolver
        uses: ./.github/actions/gh-pr-comment
        with:
          pr_number: ${{ steps.create_conflict.outputs.pr_number }}
          body: '@conflict-resolve'
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Wait for PR Update
        uses: ./.github/actions/gh-wait-for-branch-update
        with:
          branch: ${{ env.BRANCH_B }}
          initial_sha: ${{ steps.get_sha.outputs.initial_sha }}
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Wait for Checks
        uses: ./.github/actions/gh-pr-wait-checks
        with:
          pr_number: ${{ steps.create_conflict.outputs.pr_number }}
          timeout: 1200
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Verify Conflict Resolution
        env:
          GH_TOKEN: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}
          BRANCH_B: ${{ env.BRANCH_B }}
        run: |
          set -e
          git fetch origin $BRANCH_B
          # Use FETCH_HEAD for guaranteed freshness
          git checkout -B $BRANCH_B FETCH_HEAD
          # Check if the conflict is resolved by verifying the content
          FILENAME="conflict-file.generated.ts"
          if grep -q "branch-b content" "$FILENAME"; then
            echo "✅ Conflict resolved: $FILENAME has the correct content."
          else
            echo "❌ Conflict resolution failed: $FILENAME has incorrect content."
            exit 1
          fi

      - name: Close PR
        if: always()
        uses: ./.github/actions/gh-pr-close
        with:
          pr_number: ${{ steps.create_conflict.outputs.pr_number }}
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Cleanup Branches
        if: always()
        env:
          BRANCH_A: ${{ env.BRANCH_A }}
          BRANCH_B: ${{ env.BRANCH_B }}
          BASE_BRANCH: ${{ env.BASE_BRANCH }}
        run: |
          set -e
          echo "Cleaning up branches..."
          git push origin --delete $BRANCH_A
          git push origin --delete $BRANCH_B
          git push origin --delete $BASE_BRANCH

  test_pr_squash_rebase:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}
          fetch-depth: 0

      - name: Create Test Branch
        run: |
          set -e
          BRANCH_NAME="e2e-test-rebase-$(date +%s)"
          echo "BRANCH_NAME=$BRANCH_NAME" >> $GITHUB_ENV
          git checkout -b $BRANCH_NAME
          echo "test content" > test-file-rebase.txt
          git add test-file-rebase.txt
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit -m "E2E Test Commit 1 (rebase)"
          echo "another line" >> test-file-rebase.txt
          git add test-file-rebase.txt
          git commit -m "E2E Test Commit 2 (rebase)"
          git push origin $BRANCH_NAME

      - name: Create PR
        id: create_pr
        uses: ./.github/actions/gh-pr-create
        with:
          title: 'E2E Test PR (rebase)'
          body: 'Test PR for E2E testing (rebase)'
          base: leader
          head: ${{ env.BRANCH_NAME }}
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Get Initial SHA
        id: get_sha
        env:
          BRANCH_NAME: ${{ env.BRANCH_NAME }}
        run: |
          set -e
          SHA=$(git ls-remote origin "refs/heads/$BRANCH_NAME" | awk '{print $1}')
          if [ -z "$SHA" ]; then
            echo "❌ Error: Could not get initial SHA for $BRANCH_NAME"
            exit 1
          fi
          echo "initial_sha=$SHA" >> $GITHUB_OUTPUT

      - name: Trigger PR Squash and Rebase
        uses: ./.github/actions/gh-pr-comment
        with:
          pr_number: ${{ steps.create_pr.outputs.pr_number }}
          body: '@pr-squash-rebase'
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Wait for PR Update
        uses: ./.github/actions/gh-wait-for-branch-update
        with:
          branch: ${{ env.BRANCH_NAME }}
          initial_sha: ${{ steps.get_sha.outputs.initial_sha }}
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Wait for Checks
        uses: ./.github/actions/gh-pr-wait-checks
        with:
          pr_number: ${{ steps.create_pr.outputs.pr_number }}
          timeout: 1200
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Verify Squash and Rebase
        env:
          GH_TOKEN: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}
          BRANCH_NAME: ${{ env.BRANCH_NAME }}
        run: |
          set -e
          git fetch origin $BRANCH_NAME
          # Use FETCH_HEAD for guaranteed freshness
          git checkout -B $BRANCH_NAME FETCH_HEAD
          COMMIT_COUNT=$(git rev-list --count HEAD ^origin/leader)
          if [ "$COMMIT_COUNT" -eq 1 ]; then
            echo "✅ Squash and rebase successful: Branch has 1 commit."
          else
            echo "❌ Squash and rebase failed: Branch has $COMMIT_COUNT commits instead of 1."
            exit 1
          fi
          # Verify that the branch is rebased on top of leader
          if git merge-base --is-ancestor origin/leader HEAD; then
            echo "✅ Branch is rebased on top of leader."
          else
            echo "❌ Branch is not rebased on top of leader."
            exit 1
          fi

      - name: Close PR
        if: always()
        uses: ./.github/actions/gh-pr-close
        with:
          pr_number: ${{ steps.create_pr.outputs.pr_number }}
          github_token: ${{ secrets.ARI_PAT || secrets.CI_BOT_PAT || github.token }}

      - name: Cleanup Branch
        if: always()
        env:
          BRANCH_NAME: ${{ env.BRANCH_NAME }}
        run: |
          set -e
          echo "Cleaning up branch..."
          git push origin --delete $BRANCH_NAME
