'use client'

const PlaylistTracksDisplay = ({ playlistId }: { playlistId: string }) => {
  return (
    <div>
      <h2>Playlist Tracks Display</h2>
      <p>This component has been temporarily simplified to remove dead code.</p>
    </div>
  )
}

export default PlaylistTracksDisplay
